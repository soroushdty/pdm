# Updated content of funcs/preprocessing.py

# Code that replaces cfg["dataset"] with cfg["DIR_INPUT"]

# Example:
# cfg["DIR_INPUT"] = some_directory
# Use cfg["DIR_INPUT"] instead of cfg["dataset"]
